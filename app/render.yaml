services:
  - type: static
    name: mytoolbox-frontend
    buildCommand: npm run build
    staticPublishPath: ./dist  # Change to your build output folder
    envVars:
      - key: VITE_API_URL
        value: https://mytoolbox-production.up.railway.app
